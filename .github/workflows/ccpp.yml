name: Build

on: [push]

jobs:
  build:

    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v1
    - name: Building Required Dependencies
      run: make init
    - name: Build Application
      run: make
